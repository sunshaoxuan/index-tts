# 命令记录

1. 检查任务目录、进程 CPU、内存、线程等待原因和加载模块。
2. 盘点 `docker ps`、`docker system df`、镜像数、卷数和 WSL 内存。
3. 在 `docker-desktop` 内执行 `sync` 与 `echo 3 > /proc/sys/vm/drop_caches`。
4. 精确终止旧渲染解释器 PID 35800 与包装 PID 8360。
5. 重启固定端口 7864 的 Node 产品服务，并检查 `/api/health` 与 `/api/active-job`。
6. 启动真实渲染任务 `801de0037c2c4188873e7fc86ca166e2`，轮询到 complete。
7. 验证 WAV、ZIP、JSON、SHA-256、HTTP 头、浏览器 DOM、播放器、Console 和截图。
8. 运行聚焦测试、完整非 GPU 回归、Node 测试与前端生产构建。
