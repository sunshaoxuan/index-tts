# 测试结果

## Node

`pnpm test` 结果为 10 项通过。覆盖健康检查、声音和节奏指令映射、工程创建、章节与纠音保存、未知枚举拒绝、旧工程迁移、姓名占位人物数据迁移、音频媒体响应头、音色完整和 Range 206 响应、缺失音色 404、任务互斥、活动工程 PUT 409、活动任务查询、终态解锁、服务重建后存活 Worker 恢复、失联 Worker 错误终态和 Worker 失败终态。

`pnpm build` 通过。TypeScript 7 和 Vite 8 共转换 3087 个模块，生产资源生成成功。

## Python

`py_compile` 通过。

聚焦测试 38 项通过。

全量非 GPU 测试 190 项通过，22 项 GPU 标记排除，30 个子测试通过。存在 3 条第三方依赖弃用警告。

本轮固定端口启动器回归测试 3 项通过，`git diff --check` 通过。

本轮人物小传与声音导演聚焦测试合计 40 项通过，覆盖人物小传提示契约、VoiceDesign 作品体裁与角色范围上下文、旁白名称去重、自然语言声音提示、跨角色音色、真实 Worker 输入、低声说人物归属和 Windows 启动器。

## 真实运行

1. 7864 健康检查为 `ok`，Node 版本为 v24.19.0。
2. 真实渲染任务完成，7 条分句缓存全部复用。
3. 真实音色保持任务完成，4 个已有稳定音色保持不变。
4. 专用工程完成 Ollama 分析，得到 2 个角色和 5 条分句，原文覆盖完整。
5. 全新 VoiceDesign 生成 2 个永久音色，第二次运行签名复用 2 个，稳定 ID 一致。
6. 新音色渲染生成 5 条分句、1 个章节和 2 个角色轨，缓存复用为 0。
7. 桌面和移动端 UI 截图完成。
8. Console 0 error、0 warning。
9. 启动器在 7864 已被占用时终止原进程并在同一端口启动当前版本；7861、7862、7863 无监听。
10. 最新桌面端工作区滚动位置为 912px，导航 `bottom` 为 -830px，工程控制区 `top` 为 103px，重叠判定为 false。
11. 390 x 844 移动端工程控制区 `top` 为 89px，导航位于视口外，页面横向溢出为 false。
12. 角色表 1600px 和分句表 1900px 宽内容均在 1166px 表格视口中提供横向滚动；角色枚举展开成功。
13. 摄影首屏、过渡模糊、透明工作区、无重叠工作区、移动端、角色表和分句表截图均已保存。
14. 最终服务 PID 37500 监听 7864，7861、7862、7863 均无监听。
15. 原始失败请求在服务日志中确认为 `FST_ERR_CTP_EMPTY_JSON_BODY`；修复后同一白夜行工程的分析 POST 返回 202。
16. 白夜行真实任务完成 3 个文本块，耗时 188.449 秒，安全回退 0 块，输出 6 个角色和 98 条分句。
17. 98 条分句重建原文通过 `coverage_key` 完整覆盖校验。
18. 桌面端阅读层为 Bark Brown 72% 到 48% 透明渐变；移动端为 78% 到 54%。卡片和工程控制条计算背景继续为透明。
19. 最新桌面和移动端截图完成，移动端无横向溢出，Console 0 error、0 warning。
20. 生产任务运行窗口内 PUT 返回 409，任务 complete 后同一 PUT 返回 200。
21. 浏览器任务期间全文、工程选择、角色输入、角色下拉、删除与补充按钮均禁用；任务终态全部恢复。
22. 桌面和移动端前景进度面板截图完成，0% 最小活动填充可见，移动端面板完全位于视口内。
23. `voice_05.wav` 实际播放、暂停和进度定位通过，拖动到 4.2 秒后 UI 显示 `0:04 / 0:08`。
24. 下拉菜单内部从底部滚到顶部并继续滚动，页面 `scrollY` 始终为 913；滚回底部并越界后仍为 913。
25. 菜单关闭后 `select-popup-open` 被移除，`html` 恢复 `overflow: visible`；Console 0 error、0 warning。
26. 390 x 844 移动端角色表内部横向滚动到 900px 后微型播放器可见，页面根节点无横向溢出。
27. 白夜行真实音色任务刷新前后使用同一任务 `e0f20b083101444db5a7aae8665c0d32`，刷新后恢复 10% 的 `旁白 1/7` 和完整工程锁。
28. 390 x 844 移动端任务 `26d53ff6946e492c843047cae3f83ca6` 刷新后恢复 10% 的 `旁白 1/1`，工程选择和音色任务按钮均禁用，页面无横向溢出。
29. 两次刷新恢复浏览器验收后 Console 0 error、0 warning；任务终态后进度面板收起、编辑恢复、`/api/active-job` 返回 `available: false`。
30. 白夜行旧工程姓名占位在真实 DOM 中迁移为“待补充”小传和“中性清晰”预设，姓名不再作为高级声音提示。
31. 桌面角色编辑中修改人物小传、高级声音导演和重新生成开关后，最终指令同步包含大阪刑警、胸腔共鸣和角色范围限制。
32. 角色表列边界依次为 125、150、330、250、270、190px，播放器与操作列不相交。
33. 390 x 844 编辑窗口 `clientWidth=311`、`scrollWidth=311`，页面宽度和滚动宽度均为 390，Console 0 error、0 warning。
34. 真实任务 `b8965e4b87954f5db7bfa7e2a1e40b1c` 为安全回退 1 块，正确把“低声说”前的角色识别为林澈，并明确标注小传信息不足。
35. 撤回上一轮未覆盖真实长节奏标签的“角色编辑无覆盖”结论。以“沉稳舒缓 · 沉稳舒缓，重音清晰，短语间自然停连”重新验证，1280、820 和 390 像素宽度下节奏 Select 与下次生成处理矩形均不相交。
36. 820 x 900 下页面 `scrollWidth=820`；390 x 844 下页面 `scrollWidth=390`。三档截图人工查看无控件覆盖，最新 Browser Console 0 error、0 warning。
37. 本产品相关 Python 回归 40/40 通过，Node 10/10 通过，TypeScript 与 Vite 生产构建通过，Windows 启动器 3/3 通过，`git diff --check` 通过。
38. 全仓 `pytest -q` 为 192 passed、6 skipped、16 errors。错误均位于历史 `test_v1.py` 和 `test_v2.py` 模型加载夹具，分别为当前检查点向 V1 传入未知 `emo_condition_module`，以及 V2 从同一 `config.yaml` 读取到空配置；未涉及本次 React、CSS 或产品工作台测试。
39. 原老板娘和死者妻子永久音色基频分别约 89.5 Hz 和 76.7 Hz，且六个非旁白角色元数据均包含旁白专属老年男性条件，确认用户报告和全角色污染范围。
40. 真实 AI 路由首次错误地把“旁白缓慢而深沉”标为 global。新增验证器拒绝该结果，自动重做后两个原子片段均只指向 narrator。
41. 只读 qwen3:8b 语义测试正确把姓名指令分配给笹垣，并依据“经营烤乌贼饼摊位的女性”人物描述分配给老板娘。
42. 任务 `76d05463c67e46278898257b542d3695` 完成：新生成 1 个、正确签名复用 4 个、保留 2 个。所有非旁白角色最终元数据均无旁白条件；两位女性新音色基频约 165.5 Hz 和 155.9 Hz，`gender_verified=true`。
43. 浏览器实际播放老板娘新音色 1.18 秒，音频总长 5.52 秒；页面显示 qwen3:8b 语义分配、女性硬约束和无旁白条件的最终 VoiceDesign 指令；Console 0 error、0 warning。
44. 相关 Python 回归 48/48、Node 10/10、Windows 启动器 3/3、TypeScript 与 Vite 生产构建通过。新增覆盖 AI 路由、显式目标验证、女性错配重试、未验证缓存拒绝和跨轨污染音色隔离。
45. 七个历史受污染永久音色已标记 `quarantined=true`，包含原老板娘、原笹垣两个版本及其他受影响人物；隔离音色不会被签名查找或音色库列表复用，音频过程文件继续保留以供审计。
46. Node 13/13 通过，其中 3 项新增测试覆盖按稳定序号更新第 103 行、角色 ID 与名称同步、未知角色和未知序号拒绝。
47. TypeScript 与 Vite 生产构建通过。测试文件从浏览器应用编译范围排除，由 Node 24 类型剥离测试流程独立执行。
48. 产品相关 Python 回归 48/48 通过，`py_compile` 与 `git diff --check` 通过。唯一警告来自 librosa 依赖的弃用提示。
49. 白夜行真实第 103 行选择旁白后，页面显示 `旁白 narrator`，保存按钮启用；保存完成后刷新并重新打开第 6 页仍显示相同角色。
50. 项目 API 返回第 103 行 `role_id=narrator`、`role_name=旁白`。`/api/active-job` 返回 `available=false`，7864 由当前 Node 产品服务监听。
51. 截图 39 显示保存重载后的第 103 行，截图 40 显示该行完整角色选项。最终 Browser Console 0 error、0 warning。
52. 句内短引用回归将截图原三条恢复为“前一完整句、含烤乌贼饼引用的完整旁白句、后一完整句”，引号引用不再独立成轨。
53. “老板娘说声‘好了’”保持人物对白；同一输入中的“烤乌贼饼四十元”招牌引用和“多谢。”对白逐对处理，没有跨引号匹配。
54. 中文单引号、直角引号、双直角引号和 ASCII 双引号四组参数测试通过。中文双引号由白夜行真实原句和专用回归覆盖。
55. 预拆的无标点“好了”能够根据相邻说话动作和人物小传中的“老板娘”身份词继承“中年妇人”角色。“老板娘亲切地”不会成为新角色。
56. `tests/test_text_director.py` 40/40 通过。产品相关 Python 回归 58/58、Node 13/13、TypeScript、Vite、py_compile 和 `git diff --check` 通过。
57. 白夜行真实任务 `29437e3f39534deb8c236752cca1e956` 完成 5 个文本块、0 个安全回退，截图目标从三条恢复为一条完整旁白句。
58. 白夜行真实任务 `e0b8cf9a8e97490c933892c47d41b24d` 使用逐对引号解析后，两处招牌引用均为完整旁白句，“多谢。”成为独立对白；该轮进一步暴露人物小传别名继承问题并触发最终修复。
59. 最终真实任务 `30f055efd138422984cbf81496ed5b49` 完成 5 个文本块、0 个安全回退，产出 110 条分句和 7 条角色轨道。
60. 最终 API 中第 7、22 行为完整招牌旁白句，第 20、24 行“好了”“多谢。”均为 `role_002 / 中年妇人`，角色表无“老板娘亲切地”。
61. 截图 41 和 42 已人工检查。页面保留引号、自然节奏和可编辑枚举，Browser Console 0 error、0 warning。
62. 辅助命令 `ollama ps` 因 Ollama CLI 未加入当前 PATH 无法执行。该项不影响产品任务，三个真实 AI 任务均通过 HTTP 服务完成并产生终态证据。
63. 提示契约测试确认 AI 在拆句前必须结合完整句、相邻句、人物表和说话动作，判断人物对白、心理活动、句内引用或普通叙述。
64. 当前活动任务接口返回 `available=false`。不会继续对用户工程执行分析或生成任务。
65. 纯标点门禁聚焦测试为 45/45，通过句号吸收、前置省略号吸收、全标点拒绝及原有文本导演回归。
66. 产品相关 Python 最终回归为 61/61，通过小说工程、文本导演 Worker、文本导演、音色设计 Worker 和 Windows 启动器测试。唯一警告来自 librosa 的上游弃用提示。
67. Node 最终回归为 17/17，新增相邻多条合并、非相邻拒绝、跨角色与跨章节拒绝、原文光标拆分、停顿继承、无效位置拒绝和建议位置测试。
68. TypeScript 与 Vite 生产构建通过，Python `py_compile` 和 `git diff --check` 通过。
69. 首次直接运行 `pnpm test` 与 `pnpm build` 时，当前命令环境找不到 Node，测试未开始。加载工作区 Node 运行时并显式加入 PATH 后，两项重新执行并通过。
70. 误把 TypeScript 测试路径传给 pytest 的一次命令失败已纠正，后续使用 Node test runner 执行相同测试文件并通过。
71. 桌面真实验收完成合并、保存、刷新、拆分、再次保存和刷新。前半句停顿为 250 ms，后半句保留 300 ms。
72. 非相邻分句和跨角色分句合并均被前端门禁拒绝，结果数量保持 5 条。
73. 截图 43、44、45、46 已人工检查。桌面工具栏、桌面拆分窗口和 390 x 844 拆分窗口未见控件重叠，移动端没有页面横向溢出。
74. 最终构建后使用正式启动器重启固定 7864，PID 从 35244 更新为 33260。`/api/health` 为 ok，`/api/active-job` 为 false，项目 API 为 5 条。最终页面、拆分窗口和 Browser Console 复查通过，Console 为 0 error、0 warning。
75. 专用验收工程已恢复到测试前 SHA256，临时备份目录已删除。本轮没有触发 AI 分析、音色生成或完整音频任务。
76. 建议拆分位置使用与浏览器 selectionStart 一致的 UTF16 字符串偏移。新增 emoji 位于标点前的回归用例，防止增补字符导致建议位置错位。
