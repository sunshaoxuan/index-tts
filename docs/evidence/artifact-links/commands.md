# 验证命令

```powershell
node --test server\*.test.mjs src\*.test.ts
node node_modules\typescript\bin\tsc -b
node node_modules\vite\bin\vite.js build
Invoke-WebRequest -Method Head <audio-url>
Invoke-WebRequest -Method Head <package-url>
Invoke-WebRequest -Method Head <manifest-url>
git diff --check
git diff --cached --check
```

浏览器验收覆盖交付标签、三个下载按钮、三个打开链接、三个可复制绝对 URL、下载事件、Console 和截图。
