# 命令记录

1. 检查 master、远端、现行镜像、容器健康和真实工程片断数据。
2. 检索 `latest-render`、过程片断索引与前端匹配函数。
3. 在 Node 24.10 Linux 容器中执行 `corepack pnpm test`。
4. 按锁文件安装隔离 Linux 依赖后执行 `corepack pnpm run build`。
5. 执行 `git diff --check`。
6. 部署后检查健康接口、镜像 revision、容器状态、真实工程相邻片断和浏览器 Console。
