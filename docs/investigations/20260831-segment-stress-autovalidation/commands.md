# 命令记录

1. 读取调查 skill 与 IndexTTS 历史记忆索引。
2. 检查两个工作区的 Git 分支、状态、远端和最近提交。
3. 查询官方 GitHub 仓库、发行页和 Hugging Face 模型页。
4. 使用 `rg` 追踪 SegmentRow、分句再生成 API、情绪参数、候选数据和移动端样式。
5. 执行 Python、Node、TypeScript、Vite 与 `git diff --check` 验证。
6. 构建 `indextts25-product-studio:1.1.39`，检查容器健康、重启次数、健康接口与镜像内实际代码。
7. 使用独立临时工程发起真实高级分句生成，检查三份 WAV、候选评分、代理阈值、自动采用项与缓存索引。
8. 首次真实结果触发抽卡停止条件与同序号缓存去重返工，并新增对应单元测试。
9. 浏览器自动化重新加载本地地址时被 URL 安全策略阻止，保留为最终页面复测证据缺口。
