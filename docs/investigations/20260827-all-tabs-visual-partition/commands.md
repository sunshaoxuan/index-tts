# 命令记录

1. 检查 Git 分支、状态、最近提交和 7864 监听进程。
2. 检索 `product-studio/src/App.tsx`、`product-studio/src/styles.css` 和现有测试中的页面结构与样式契约。
3. 使用内置浏览器访问 `http://127.0.0.1:7864/`，建立六个页签的桌面基线。
4. 执行 `pnpm test`、`pnpm run build` 和 `git diff --check`。
5. 每次构建后重启 7864 服务，避免静态资源哈希与运行服务不一致。
6. 在 1440×1000、980×900、390×844 下逐页点击六个页签，记录页面宽度、表体宽度、分页行数、交替底色、Console 和截图。
7. 测量当前字幕修改前字号、动画和横向边界，记录 10px、13px、`none` 和 637=637。
8. 执行当前字幕专项测试、全量测试和生产构建，首次为 93 项，手机返工后最终为 94 项。
9. 使用 `docker compose up -d --build studio` 完整重建运行镜像并确认健康；手机返工后同步最终 `dist` 到同一容器并重启静态服务，使新哈希资源生效。
10. 在三视口测量当前角色、正文、字幕宽度和页面宽度并保存 `delivery-caption-emphasis.jpg`。
11. 拖动进度条并实际播放，验证当前字幕索引从 90 推进到 96。
12. 在标准动态验收版本捕获 0.4 秒动画计算样式，恢复正式减少动态效果规则后重新执行测试、构建、部署、健康检查和 Console 检查。
