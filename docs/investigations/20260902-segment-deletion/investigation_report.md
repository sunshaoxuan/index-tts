# 分句删除功能调查与实现报告

## 目标

在分句导演中删除无需朗读的注释类分句，保留完整正文原稿，并保证后续分句编号、缓存复用、完整交付状态和工程保存行为一致。

## 行为链

1. React 表格以稳定分句序号保存跨页选择。
2. `deleteSegmentsByOrder` 校验所选序号、移除目标记录并连续重排剩余分句。
3. 页面清除临时选择和拆分状态，登记工程未保存状态。
4. 工程 PUT 保存后，服务端以不含序号的完整分句身份比较新旧记录。
5. 被删除记录的过程缓存被移除，包含该记录的完整交付被标记过期。
6. 原文、合成文字、角色、语言和导演参数未变化的后续片断继续匹配，并迁移到当前序号。

## 已验证结论

| 结论 | 证据 | 置信度 | 限制 |
|---|---|---|---|
| 工具栏支持一个或多个已选分句删除 | `App.tsx`、`segmentRowLayout.test.ts`、桌面和窄屏确认截图 | 高 | 浏览器确认层在自动化中取消，未直接点击危险确认按钮 |
| 删除保留正文原稿 | 确认文案、需求文档、运行 API 与刷新后原稿文本域检查 | 高 | 测试工程已在验收后清理 |
| 剩余分句连续重排 | `segmentState.test.ts`、运行 API 返回 `[1,2]`、刷新后的浏览器行 | 高 | 无 |
| 被删缓存失效且后续片断保留 | `server/index.test.mjs` 的删除回归测试 | 高 | 使用隔离临时工程和测试 WAV |
| 桌面和 390×844 布局无功能性遮挡 | 三张截图、窄屏 `scrollWidth 375 <= innerWidth 390` | 高 | 未覆盖所有浏览器内核 |
| 页面控制台无错误和警告 | 删除确认前后 `tab.dev.logs` 均为 0 | 高 | 仅覆盖本次专用工程流程 |

## 失败与修正

- 首次建立测试分句时，新建工程没有默认角色，服务端以“分句 1 引用了未知角色”拒绝保存。读取工程真实角色表后显式加入合法旁白角色，再建立测试数据。
- 首次浏览器复选框定位使用快照展示名称，没有匹配实际可访问名称。改为以注释原文限定表格行，再定位行内唯一复选框。
- 原稿文本域的精确占位定位在页签切换动画中超时。改为读取当前页签文本域值集合并核对注释原文。

## 运行边界

- 当前生产容器 `indextts25-product-studio:1.1.64-9592d6d` 未替换。
- 本次浏览器验收使用 `127.0.0.1:7865` 当前源码服务。
- 专用测试工程已删除，7865 服务已停止，7864 容器验收后继续健康。
