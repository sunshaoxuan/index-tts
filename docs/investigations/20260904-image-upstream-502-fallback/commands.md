# 命令记录

```powershell
git status --short
git diff --stat
git diff --check
node --test server/image-model-routing.test.mjs server/index.test.mjs src/api.test.ts
pnpm test
pnpm exec tsc --noEmit
pnpm build
docker compose config --quiet
```

部署与最终 Git 验收命令将在执行后补充。
