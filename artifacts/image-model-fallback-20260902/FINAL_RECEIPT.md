# 最终回执

## 交付内容

* 主图像模型、互补图像模型和冷却切换设置。
* GPT Image、Gemini Image 和普通兼容图像模型的提示词规格。
* 分镜批次模型选择与跨模型风格警告。
* 429、503、配额和临时过载切换，普通错误保持失败。
* 角色参考图在模型切换后的完整上传和身份审计。
* 镜头请求模型、实际模型、切换原因和提示词规格持久化。
* README、CHANGELOG、需求文档、架构文档与证据文档。

## 运行回执

生产地址为 `http://127.0.0.1:7864/`。容器健康状态为 `healthy`，应用健康接口返回 `ok`。运行配置在验收后保持原主模型 `gemini-3.1-flash-image`，互补模型为空，开关关闭。

最终复验期间页面存在一个独立的 AI 分镜重新生成后台任务，工程按产品规则处于锁定状态。本次交付没有取消、修改或重启该任务。

## 验收边界

真实付费图像生成未执行。外部模型输出质量与两个模型之间的实际视觉一致性为 `evidence_missing`。路由协议、参考图传递、审计字段和 UI 已通过 mock、自动化测试与生产页面验收。
