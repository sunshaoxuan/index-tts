# Changelog

* 分句导演支持勾选删除一个或多个无需朗读的注释类分句。删除前显示影响范围并二次确认，正文原稿继续保留；保存后被删片断缓存与相关完整交付按既有规则失效，未变化的后续音频迁移到连续新序号。

* 修复角色替换保存完成时出现全页面刷新感的问题。保存期间只锁定替换弹窗和背景交互，不再挂载全屏加载层，角色与分句持久化、慢网防重复和失败重试边界保持不变。

本文件记录 IndexVoiceStudio 的产品版本。版本格式遵循 [Semantic Versioning](https://semver.org/)。IndexTTS 推理引擎保留独立版本号。

## [Unreleased]

### Added

* 标准角色参考样本增加可调声音时长系数。自然和沉稳舒缓继续作为快捷预设并分别载入 1.05 与 1.18，角色卡片同时提供 0.80 至 1.40 的滑块和数字输入，手动调整后标记为自定义；服务端按 IndexTTS 2.5 原生 0.50 至 2.00 范围校验，三个全门禁候选统一保存并使用本次实际系数。
* 角色资产卡片增加标准角色参考样本生成。系统始终从不可变的原始上传音源生成自然或沉稳舒缓的三版候选，最多尝试三十次并在收齐三版后立即停止，显示基础音频质量、CAMPPlus 音色相似度和回声代理门禁。候选不会自动采用，只有通过全部门禁的版本可由用户试听后采用；采用会原子保存并使相关旧片断失效，也可随时恢复原始上传样本。生成期间角色卡片保持锁定，支持取消和页面刷新后的任务恢复。
* 分句导演增加节奏批量修改。可将“舒缓”等节奏一次应用到跨分页选中的分句或当前工程全部分句；全量操作显示影响数量并二次确认，界面明确提示“舒缓”会启用慢速提示和 1.18 时长系数。
* 图像生成增加主模型与互补模型路由。全局 AI 设置可分别配置 `gpt-image-2`、`gemini-3-pro-image` 等兼容服务实际暴露的图像模型，并控制限流、配额冷却或临时不可用时是否切换。分镜工作区可为当前单张或全量批次固定请求模型，并明确提示跨模型风格差异。GPT Image、Gemini Image 和普通兼容模型使用各自的提示词执行规格，同时保留统一人物身份、构图、文字和单画面约束。每张结果保存请求模型、实际模型、切换状态、切换原因和提示词规格；含人物镜头在切换后仍通过 Images Edits 上传全部原始角色参考图，参考图错误不会触发模型切换。
* 所有长时间操作增加可见取消入口。单分句音频、完整音频、角色音色、全文分析和 AI 分镜可以取消等待中或运行中的后台任务；人物小传扩写、角色形象、单张与全量关键帧、AI 服务测试和工程读取可以中止当前 HTTP 请求。音频 Runtime 取消会先核对任务归属，取消后解除锁定并保留此前完成的可恢复成果。
* 全量关键帧生成增加可观察的逐镜头任务流程。系统先预检全部镜头和人物参考图，再按稳定顺序逐张请求并立即回写；页面实时显示已完成数量、总数、百分比、当前镜头、已用时间和预计剩余时间。生成期间锁定分镜编辑、工程切换、保存、AI 设置和冲突操作，并提供单张与全量取消入口。完成、取消或失败后自动解除锁定，已成功图片继续保留，失败状态标明具体镜头。
* 分镜关键帧增加角色身份参考图机制。镜头参与人物按稳定角色 ID 读取当前工程或关联工程中的原始角色形象，通过 Images Edits 多图输入约束面部结构、五官比例、发型、年龄感和标志特征；旁白不进入图片参考。单张与全量生成共用同一解析和预检链路，缺少角色形象时在请求前停止，兼容服务拒绝多图编辑时明确报错。工程保存参考模式、角色 ID、名称、角色图 URL 和 SHA256，页面逐镜头显示已使用、已就绪或待补齐状态。
* 视频分镜升级为场景与镜头两层结构。一个场景可包含多个短镜头，每个镜头对应一张关键帧；已有音频时按真实分句时长和停顿以默认约 10 秒目标拆分，支持 5 至 20 秒预设、逐镜头真实时间显示、AI 全量重建、手工创建、镜头拆分、相邻镜头合并、单张重新生成和全部镜头图片生成。AI 全量重建保留稳定角色、当前分句、人工朗读文字、音频、纠音和导演记忆。
* 角色资产卡片支持上传 WAV、MP3、FLAC、M4A、AAC 和 OGG 作为参考音频。服务端校验真实格式并转换为 24 kHz 单声道 PCM WAV，应用并保存角色设置后，该角色全部分句统一使用上传音色；慢速上传期间角色卡片保持锁定并显示处理状态。
* 项目操作工具栏改为手工展开与收缩，页面滚动、顶部导航和工作区切换保持当前显示状态，十秒闲置后继续自动隐藏。工具栏新增全局 AI 设置和保存当前工程，并根据当前工作区只启用对应的全文分析、角色音色或完整音频生成动作。
* 角色资产卡片增加“替换为已有角色”。确认后删除重复的新角色，把分句导演、AI 文档分句、场景参与人、说话人候选、导演补充路由、导演记忆和关联工程映射统一切换到目标角色；目标角色的资料与音色保持不变。源名称和别名会并入目标角色别名，后续 AI 全文分析可复用这次人工确认，减少同一人物因名称差异再次新增。
* 视频分镜式场景工作台。AI 按内容主题、地点、空间方位、观察方向和叙事焦点建立场景边界，撰写可直接用于图像生成的场景小记，并记录起止分句。已有完整音频时，页面按真实片断时长和停顿显示场景开始与结束时间。
* 场景关键帧生成。提供八种插画分镜风格和电影写实风格，支持重新生成单张与重新生成全量，工程保存关键帧地址、提示、风格、模型和生成时间。
* IndexTTS 与 VoiceDesign 模型统一挂载到位于 C 盘 NVMe SSD 的只读外部 Docker 卷。后台任务面板统一显示当前模型运行时、进程存活、加载阶段、内存、累计磁盘读取量和模型总字节数，供首次加载与驻留复用验收。
* 新建声音工程时可多选关联已有工程，导入来源工程的全部角色资料、当前音色、全部候选音色和全篇纠音规则。新工程保存来源工程、角色 ID 映射、可用音色、缺失音色以及纠音重复和冲突回执；多个来源中的同名角色 ID 会分配独立目标 ID，同原词纠音按来源选择顺序保留首项，导入完成后各工程独立维护。
* 本地 Ollama 全文分析默认模型调整为 `qwen3:14b`，当前全局设置同步切换到该模型。
* 分句片断播放器增加加载中、可播放、缓冲和失败状态，并提供手工重新加载入口。
* 最近一次交付的完整音频播放器增加读取、缓冲、可播放、播放中、暂停和失败状态，慢速网络等待期间显示缓冲百分比与自动播放提示，失败后可重新加载媒体请求。

### Fixed

* 修复分句高级三版后两版可能明显偏离角色参考音色的问题。三版现在始终使用角色当前同一个参考 WAV，并关闭会随机替换情绪嵌入原型的 `use_random`；候选差异继续由合成采样产生。分句 CAMPPlus 音色门槛提高到 0.82，候选必须同时通过音质、参考音色和适用重音门禁才能进入三选一，九次仍不足三版时保留原片断并报告失败。同一分句保留旧已采用片断并生成新待选三版时，页面优先展示后写入的新候选集合。
* 修复角色音色抽取在尝试预算内只得到一至两个合格候选时整组丢弃，以及再次执行仍重复同一批种子的问题。Worker 现在立即注册每个通过年龄、性别和目标频率门禁的自然候选；角色资产保存累计尝试数和未补齐状态，下一轮只生成缺少的数量并从新的连续种子区间继续。任务结果分别统计声学年龄或性别失败与目标频率失败，便于判断未补齐原因。
* 修复兼容平台返回 Gemini 图像模型却不提供 Images API 时，角色形象生成直接返回 404 的问题。Gemini Image 现在通过 Chat Completions 请求图像响应，GPT Image 与普通兼容模型继续使用 Images API，并兼容 Markdown Data URL、图像数组和 inline data。
* 修复图像供应商网关返回 500、502、504、请求超时或连接中断时未切换互补模型的问题。临时上游故障现在进入冷却并尝试另一个已配置模型；上游 5xx 对浏览器使用 424 JSON 错误透传，避免公网反向代理替换错误体。非 JSON 页面提示会明确指向反向代理或上游生成服务。
* 修复人物审校只要求修改 `age_basis` 时，说明文字中的年龄数字被误判为必须修改年龄值，导致快速复核连续重做后整次分析失败的问题。年龄值门禁现在只响应明确的目标年龄或明确声明年龄字段错误的 issue，证据类型修正可以直接验收。
* 修复小说和故事的第一人称旁白已经由全文语境推断为男性或女性，角色资产仍显示“性别待定”的问题。旁白现在根据自述身份、伴侣关系、称谓和叙事语境保存性别依据并同步角色资产；全部线索仍无法形成结论时，只对未决角色执行一次紧凑性别建议任务，失败后保存明确标记为需人工确认的系统建议。

* 修复 AI 文本导演已完成逐段解析，却因人物审校模型反复报告“原文未明示年龄”或漏选年龄证据枚举而整单失败、工程最终保持空白的问题。人物已经使用推断型证据、保留非空年龄证据，且审校问题只重复要求处理“未明示年龄”时，系统按既有人口属性规则协调为通过。当前文章的时间点年龄被错误标为 `current_explicit` 时，系统降级为 `current_inference` 并补充推断说明；整数年龄和非空证据对应的 `unknown` 缺省值同样规范化为 `current_inference`。具体年龄目标冲突、证据缺失和其他人物问题继续由校验门禁拒绝。
* 降低本地 AI 文本导演的结构化输入和输出消耗。全文角色与场景注册完成后冻结稳定 ID，正文按确定性编号原文单元逐块处理，模型只返回归属、场景和导演标注，程序回填原文、朗读文本、顺序、章节、人物名称和类型；分块提示不再重复完整 JSON Schema、人物详情和场景详情。人物复核改为每批最多两人的快速小任务，只携带相关证据，失败时只重做当前批次，已落实的修正由程序验收后直接接受。对 1581 字《成都粉子》的最终只读实测为 6 个正文块全部成功、0 个兜底、原文覆盖 100%，总输入 17196 token、总输出 5718 token、模型耗时 82.958 秒。
* 修复标准角色参考样本使用较强舒缓情绪引导造成音色身份漂移、三十次生成仍无法稳定收齐三版的问题。标准样本改用身份优先的 0.25 情绪引导强度和 0.60 采样温度，继续保持三项自动门禁、三版原子交付和三十次失败上限。
* 标准角色参考样本的三版候选改为原子交付集合。历史任务只有一版或两版通过全部门禁时不再显示孤立候选的播放器和采用操作，页面显示实际合格数量并要求重新生成；新任务继续只在收齐三版全门禁合格候选后成功。
* 修复标准角色参考样本把未通过音色或回声门禁的尝试显示为可试听候选的问题。页面现在只显示通过全部自动门禁的标准候选，全部失败时明确提示重新生成；Worker 最多尝试三十次并收齐三版全门禁合格结果后才更新角色资产，失败尝试仅写入任务审计结果并清理临时音频。
* 优化本地 AI 文本导演长时间停留在首块和 0% 的问题。分析 worker 先以统一的 8192 上下文窗口预加载 Ollama，再把全文按自然边界预拆为不超过 300 字符的任务块并逐段解析；体裁判断、全文注册和正文导演复用同一驻留模型，避免上下文参数变化触发 14B runner 重复装载。冷启动继续保留 600 秒窗口，驻留后的单块请求限制为 120 秒且只做一次同尺寸尝试，超时或校验失败后及时细分。任务状态先报告拆分总数，随后持续报告逐段序号；动态细分增加总块数时保持百分比单调，并为全文分析、人物校验和补充路由使用单调递增的阶段进度。
* 修复 AI 全量分镜把同一场景小记复制到场景内所有镜头、导致关键帧提示高度重复的问题。镜头拆分阶段现在只建立稳定 ID、分句范围、真实时间和完整对应原文，文本导演随后按小批次逐镜头撰写独立标题、画面小记、原文取景证据和实际可见人物稳定 ID。返回结果必须完整覆盖稳定 ID，取景证据必须属于当前镜头原文，画面人物必须已登记且排除旁白；原文明示姓名和场景内唯一代词连续指向通过确定性解析强制并入人物列表并保存解析来源，避免模型漏填人物数组后丢失角色参考图。过短或完全重复的画面小记会终止更新且不覆盖旧分镜。生图提示明确包含当前镜头原文。页面逐镜头显示对应原文与 AI 取景证据；手工拆分或范围缩小时清空已失效的旧画面小记和关键帧，避免重复描述继续生成图片。
* 修复拆分分句后生成当前句会清空下一句已有音频的问题。过程片断不再按可能漂移的历史序号提前去重，所有片断统一按当前原文与合成文本一对一对齐，后续未修改分句的音频会保留并迁移到当前序号。
* 修复分句导演中原文与合成文本区域高度不一致的问题。桌面布局现在由原文自然高度统一两列高度，较长合成文本在等高编辑区内滚动，不再固定为两行。
* 修复项目操作面板中的工程级生成按钮受当前工作区限制的问题。AI 全文分析、角色音色生成和完整音频生成现在从任意工作区均可提交，仅依据真实数据前置条件和后台任务锁决定可用性；提交成功后自动进入对应结果工作区，禁用时显示具体原因。
* 修复分镜镜头勾选状态只能保留同一场景内最后一项，导致相邻镜头合并按钮无法启用的问题。同一场景现在可以累积选择多个镜头，切换到其他场景时会清除上一场景的选择。
* 修复分句高级生成的音色漂移和误导性“质量通过”。候选现在使用角色当前样本执行 CAMPPlus 音色相似度门禁，分别披露基础音频、音色、重音代理和人工导演试听状态；分句节奏覆盖冲突的角色节奏并进入结构化时长系数；已有片断在使用者明确采用前继续保留。系统门禁只提供提示和排序，待复核候选仍可由使用者试听后人工采用，并保存人工覆盖审计。
* 修复慢网络下“生成本分句”和“重新生成本分句”在服务器返回前没有明显状态、可以重复点击的问题。按钮现在点击瞬间进入高对比加载状态，通过同步前端互斥锁阻止重复提交，并分别显示正在保存工程或正在提交目标分句；失败后解除锁定并提供可重试错误。
* 修复慢网络下角色替换没有操作锁定和持久化完成反馈的问题。确认替换后页面与弹窗会持续锁定并显示保存中状态，禁止重复确认、取消、关闭和背景误操作；工程保存成功后才关闭弹窗并移除源角色，保存失败时保留源角色和选择并明确提示重试。
* 修复 Linux Docker 完整渲染内存门禁把完全空闲页当作全部可用内存，忽略可回收文件缓存并误报内存不足的问题。Linux 现在读取 `/proc/meminfo` 的 `MemAvailable`，读取失败时才回退到系统页计数。
* 修复目标基频中位数只参与自然语言提示和事后排序、调整滑块却不能约束生成结果的问题。真实试听发现生成后相位声码器校准会造成回音和严重音色变化，当前实现已撤销全部电子变调。系统只登记落盘原始 WAV 进入动态目标容差且通过年龄与声音身份门禁的自然样本；目标值会映射成较低、中间或较高自然音域类别，高音男性目标明确使用自然男高音和较高日常说话音域；偏离目标后的重试使用聚焦短指令、扩大后的模型原生采样覆盖和独立审计种子序列，避免长人物背景稀释频率约束；产品与 VoiceDesign 环境锁定相同声学分析依赖并在运行时校验版本；永久音色保存原始实测频率、目标偏差、零半音修正、方法 `none` 和自然门禁版本。
* 恢复角色音色生成后的人工候选定稿流程。默认三个通过年龄与性别校验的候选全部保留并集中展示，生成任务不再自动改写角色当前稳定音色；全文分析新增角色不再随机复用全局音色库中的其他角色声音。
* 儿童 VoiceDesign 身份约束改为未变声男童或女童，移除会诱导成年声线的通用男性或女性共鸣措辞，并为默认儿童试听文本明确年龄和放学场景。单个角色未取得完整合格候选时保留其他角色已成功生成的候选和失败明细。
* 将十三岁以下候选验证拆分为儿童声区门禁与男童或女童身份确认。页面按实际证据显示儿童声区和身份确认状态，人工采用会记录试听身份确认；日语、英语、西班牙语和中文儿童默认试听文本均加入年龄及男童或女童身份锚点。
* 儿童 VoiceDesign 进一步改用经真实模型验证的简洁正向童声模板，不再混入长人物小传、旧“低沉”声音导演或成人化节奏。候选声学验收严格使用角色资产的基频上下限，防止 139 Hz 左右的成年声线被十岁角色门禁接受。
* 待定稿列表只显示当前稳定音色为空且已有合格候选的角色，已有稳定音色的导入角色不再被误列为需要重新定稿。

* 修复 AI 分轨覆盖校验失败后，自适应安全拆块把句末右引号单独分块并导致整篇分析失败的问题。句末引号和尾随空白现在保留在前一文本块中。
* 修复干净 Docker 镜像不含本地示例 WAV 时，AI 分句完成后因角色分配音色表为空而丢弃整篇结果的问题。角色分配现在使用工程当前音色、候选音色、工程音色文件和正式音色库。
* 关联工程接收新文章并完成 AI 分析时，按角色类型与标准化名称合并已有角色和 AI 角色。重叠角色复用稳定 ID、完整资产与音色，新角色追加到资产库，未在本篇出现的既有角色继续保留，全文分句统一改写为合并后的角色 ID。
* 应用增量镜像复用基础镜像中已经验证的生产 `node_modules`，构建时强制核对 `package.json` 与 `pnpm-lock.yaml` 完全一致，继续完整替换应用源码和前端生产 bundle，避免 Docker Desktop 重复复制相同依赖树时停滞。
* 本地 14B 全文分析请求超时由 300 秒调整为 600 秒，允许 Ollama 在冷启动时完成 41 层全 GPU 加载，避免客户端先断开并触发模型加载取消。
* 主画面产品名称旁以小字号显示后端当前运行版本，运行版本统一为 `1.1.5`。
* Python 文本导演的代码级兜底模型统一为 `qwen3:14b`，与 Product Studio 设置和任务输入保持一致，避免漏传配置时回落到 8B。

* 修复 Linux Docker 读取 Windows 风格交付清单路径时把反斜杠编码为 `%5C`，导致已有片断返回 404 和播放器显示 `0:00 / 0:00` 的问题。

## [1.1.0] 2026-08-27

### Added

* NVIDIA GPU Docker 镜像与 Compose 部署，支持本机加载、健康检查和 `unless-stopped` 开机恢复。
* 模型、用户工程、运行配置和成果物四类持久挂载，支持连同镜像迁移和样本发布。

* 完整音频播放器的角色与内容滚动字幕，使用真实分句 WAV 时长和句后停顿建立时间轴。播放与拖动进度时，当前字幕同步高亮并滚动到可视区域。
* 产品级全局 AI 设置，统一选择本地 Ollama 或 OpenAI 兼容全文分析模型，并管理人物小传和角色图像模型。
* 全文角色别名与场景注册、说话人候选置信度和证据字段，以及可人工修订的场景复核页。
* 角色删除操作。被删除角色引用的分句会重分配给旁白，活动角色资产和导演记忆同步清理。
* 分句导演金标准评测工具，计算边界精确率、召回率、F1 和完全相同区间的字段一致率。
* 工程控制区增加带工程名称和完整范围确认的删除工程操作，删除后自动切换到剩余工程，永久音色库继续保留。
* 一人一卡的角色资产工作台，集中编辑性别、年龄、详细人物小传、声音特征、频率目标、人物形象和声音样本。
* 按性别与年龄计算的建议基频区间，以及用于区分同类角色声音的目标基频滑块。
* OpenAI 兼容人物小传和图像服务配置，支持独立填写 Endpoint、API Key、文本模型和图像模型。
* AI 详细人物小传扩写、角色形象生成和工程专属角色图像存储。
* AI 人物与图像服务连接测试、兼容模型发现，以及可搜索、可手工输入的人物小传和图像模型选择器。
* 本机 Cockpit Instance ID、Responses API 文本调用、入口能力隔离和公网 HTTP Bearer Key 默认阻断。
* 角色形象风格预设，内置 13 种漫画方向和独立真人写实选项，默认生成漫画角色。
* 每角色独立的十二项结构化声音特征，包括声音重量、亮度、共鸣位置、声带状态、粗糙度、气息量、鼻音、吐字、语速、停顿、音高起伏和情绪外放。
* 稳定、平衡、探索和高级自定义生成策略，开放主采样与 Subtalker 的 Temperature、Top K、Top P、重复抑制、随机种子、最大生成 Tokens 和采样开关。
* 每角色专属试听文本、一至六个候选、全部候选音频保留、逐候选基频记录和候选试听切换。
* 儿童、成年和老年真实 VoiceDesign 年龄差异验证脚本及声学验收报告。

### Changed

* 移动端工作区页签改为两列多行导航，六个页签入口始终可见并可直接切换。
* 分句导演相邻记录采用更明确的深浅交替底色和实体分隔线，第二层导演字段增加独立内嵌表面。
* 分句导演字段标签提升到 12px，主要字段值和原文提升到 14px，同时增强辅助说明的字号和文字对比度。
* 分句导演从十三列横向宽表重排为每条分句上下两层的复合行，完整保留角色、文本、表演、节奏、片断试听和逐句重新生成，并取消表格横向滚动。
* 工程控制区和六个产品页签统一为四级信息分区，增强主工作区、嵌套卡片、提示区、表格、播放器、滚动字幕、成果物和空状态的暖色表面与边界，并保持桌面与移动端一致层级。
* AI 分句 Schema 直接使用产品态度和句内节奏预设，详细人物小传移出分句请求的输出预算。
* 全文分析任务从硬编码 `qwen3:8b` 改为读取全局 Provider、模型、Endpoint、接口模式和块长度。
* AI 全文分析、角色音色生成和完整音频生成组成可拖放操作面板，可吸附到画面上、右、下、左四边并保存停靠位置。项目区滚动和标签切换会展开，连续 10 秒无交互后以动画收缩到当前边缘，点击收缩图标可平滑展开。
* 角色区域从横向轨道表改为响应式人物卡片，声音样本不再被称为角色轨道。
* 角色卡片头像改用 4∶3 画幅和上移的人脸焦点，减少竖版人物图对额头、头顶和肩部的裁切。
* VoiceDesign 指令加入显式性别、年龄、建议频率、目标频率和分年龄段音色质感约束。每个角色按当前配置评估一至六个候选，并保存逐候选测量与选择记录。
* 角色年龄变化会加载对应年龄段的声音特征建议组合，用户仍可逐项修改。
* 候选评分改为执行角色配置的候选数量，所有候选在同一驻留模型中连续生成。
* 性别基频门禁改为年龄感知，儿童和青少年使用适合变声阶段的宽容区间。
* AI 全文分析的人物小传目标扩展为 300 至 600 个中文字符，覆盖声音设计、人物形象、插图和视频关键帧所需的稳定人物信息。

### Fixed

* 修复分句导演每页数量被固定为 20，导致 10、50、100 条每页选择无效的问题。分页现在显式保存页码和页大小，显示当前范围与总数，并在数据变化后校正有效页码。
* 修复角色候选三选一后，当前稳定音色文件存在却未登记在工程 `voice_files`，导致全量声音重新生成报告音色 ID 不存在的问题。分句继续只引用角色 ID，渲染按角色当前音色动态解析永久音色库，保存时自动回填角色当前音色资产。
* 修复旧工程重新加载后仍未把当前角色音色写回 `project.json`，以及常驻 Render Runtime 在代码更新后继续执行旧版渲染逻辑的问题。工程读取现在原子持久化资产回填，运行时通过源码指纹自动换代。
* 修复 Node 产品服务重启后丢失仍在 Render Runtime 中执行的长篇渲染，随后把正常忙碌状态误报为旧运行时停止超时的问题。服务现在校验并接管原任务，继续显示进度、锁定工程并保留已生成片断。
* 修复女性角色生成结果仍呈男性声线，以及未通过女性门禁的候选被注册并显示的问题。女性身份约束现在位于 VoiceDesign 指令开头并在结尾再次确认。
* 女性基频门禁按年龄和用户目标动态收紧。候选数量现在表示通过性别校验后可选择的样本数量，显式性别角色最多执行请求数量三倍的内部尝试。
* 永久音色加入性别校验版本。旧验证版本的显式性别缓存不会复用，角色卡片只显示校验通过的候选并标记校验结果。
* 修复角色自身声音描述与其他角色导演补充出现相同短语时，稳定音色被错误隔离并重新生成的问题。
* 修复成熟和老年角色在首个候选仅满足宽泛基频容差时过早结束，导致低频达标但年龄感约束不足的问题。
* 修复不同采样参数共享相同音色缓存签名的问题。
* 修复尚未变声的男童高音可能被成年男性固定基频门禁误判的问题。

### Security

* 兼容服务 API Key 只保存在被 Git 排除的本机运行目录，读取接口不会回传密钥正文。
* 页面在人物小传扩写和形象生成前说明发送到外部兼容服务的数据范围。

## [1.0.0] 2026-08-26

首个正式公开版本。

### Added

* React 19、Ant Design 6 和 Fastify 5 构成的本地长文本声音制作工作台。
* 小说、故事和新闻工程的完整原文、章节、人物、分句、纠音和导演参数持久化。
* Ollama `qwen3:8b` 全文导演、角色识别、语义断句和导演补充路由。
* Qwen3 TTS VoiceDesign 角色音色生成、模型健康门禁和永久音色库。
* IndexTTS 2.5 常驻渲染运行时和连续逐句生成模型复用。
* 分句合并、拆分、合成文字编辑、角色分配、情绪、节奏和停顿控制。
* 已生成片断加载、逐句重新生成和严格缓存串接。
* 导演操作历史记录和稿件调整后的智能重应用。
* 完整 WAV、章节音频、角色分轨、ZIP、JSON 清单和实时 MP3 下载。
* 完整交付过期标记、保留与用户确认删除。

### Changed

* 分句和纠音变化按实际影响范围使缓存失效，未受影响片断继续复用。
* 合成文本修改立即进入未保存状态，保存结果在工程控制区和分句导演区可见。
* 产品版本与 IndexTTS 引擎版本分离，IndexVoiceStudio 从 `1.0.0` 开始使用语义版本。

### Fixed

* 修复 Qwen TTS 环境或模型文件缺失时错误出现过晚的问题。
* 修复 Windows 运行时 stale PID、模型重复加载和 GPU 模型内存竞争。
* 修复同一秒内快速连续生成时交付目录名称碰撞的问题。
* 修复旧交付片断按历史序号绑定到新分句的问题。
* 修复中文交付名称用于 MP3 下载响应头时的兼容问题。
* 修复分句导演滚动越界带动页面和保存按钮状态不明确的问题。

## [0.9.0] 2026-08-25

### Added

* 持久化小说工程、版本化完整交付和任务恢复。
* React 与 Ant Design 产品界面、角色试听、分句分页编辑和移动端布局。
* 人物小传、声音条件、角色导演和性别约束。
* 分句合并、拆分、纯标点门禁和短引用语义处理。

### Fixed

* 修复刷新后活动任务丢失、角色编辑跨页错位和控件重叠。
* 修复 AI 分句覆盖不完整、引号片断误拆和导演补充进入错误角色。

## [0.8.0] 2026-08-24

### Added

* AI 长文本导演、角色分析、分句和 Qwen3 TTS VoiceDesign 链路。
* 长文本分块、覆盖验证、自动重试和安全恢复。

### Fixed

* 修复全文导演结果遗漏、改写或重排原文时缺少恢复路径的问题。

## [0.1.0] 2026-08-13

### Added

* Windows IndexTTS 2.5 本地运行环境。
* 初始生产声音工作台、示例音色和情绪控制。
* 本地模型、输出和运行目录的 Git 排除规则。

[1.0.0]: https://github.com/sunshaoxuan/IndexVoiceStudio/compare/v0.9.0...v1.0.0
[0.9.0]: https://github.com/sunshaoxuan/IndexVoiceStudio/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/sunshaoxuan/IndexVoiceStudio/compare/v0.1.0...v0.8.0
[0.1.0]: https://github.com/sunshaoxuan/IndexVoiceStudio/releases/tag/v0.1.0
